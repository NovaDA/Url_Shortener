[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
flask-sqlalchemy = "*"

[dev-packages]

[scripts]
dev = "bash -c \"export FLASK_ENV='development' && flask run\""
init_db = "python init_db.py"

[requires]
python_version = "3.10"
